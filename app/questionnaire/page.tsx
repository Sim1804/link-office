"use client";
import { useEffect, useState } from "react";
type Question={id:string;text:string;position:number};
export default function QuestionnairePage(){const[questions,setQuestions]=useState<Question[]>([]);const[answers,setAnswers]=useState<Record<string,number>>({});const[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/questions").then(r=>r.json()).then(d=>setQuestions(d.questions));},[]);async function submit(){setBusy(true);const assessment=await fetch("/api/questionnaire/start",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId:"demo-user"})}).then(r=>r.json());await fetch("/api/questionnaire/save",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({assessmentId:assessment.id,consentInformation:true,consentResearch:true,consentParticipation:true,demographic:{gender:"Préfère ne pas répondre",ageRange:"25 à 34 ans",country:"France",department:"75",occupation:"Étudiant",relationshipStatus:"Célibataire",children:false,livingSituation:"Seul",selectedSituations:[]},answers:Object.entries(answers).map(([questionId,value])=>({questionId,value})),adaptiveAnswers:[]})});const result=await fetch("/api/questionnaire/submit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({assessmentId:assessment.id})});if(result.ok)window.location.assign("/resultats/demo-user");else setBusy(false);}return <main className="min-h-screen bg-slate-50 py-12"><section className="mx-auto max-w-3xl rounded-3xl bg-white p-8 shadow-sm"><p className="font-semibold text-indigo-600">LINK OFFICE · IQRH</p><h1 className="mt-2 text-3xl font-bold">Questionnaire relationnel</h1><p className="mt-2 text-slate-600">{Object.keys(answers).length}/30 réponses</p><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-indigo-600" style={{width:`${Object.keys(answers).length/30*100}%`}}/></div><div className="mt-8 space-y-6">{questions.map(q=><fieldset key={q.id} className="rounded-xl border p-5"><legend className="font-medium">{q.position}. {q.text}</legend><div className="mt-4 flex gap-2">{[1,2,3,4,5].map(value=><label key={value} className="cursor-pointer rounded border px-3 py-2"><input type="radio" name={q.id} onChange={()=>setAnswers({...answers,[q.id]:value})}/> {value}</label>)}</div></fieldset>)}</div><button disabled={busy||Object.keys(answers).length!==30} onClick={submit} className="mt-8 rounded-xl bg-indigo-600 px-6 py-3 font-semibold text-white disabled:opacity-50">{busy?"Calcul en cours…":"Voir mes résultats"}</button></section></main>}
