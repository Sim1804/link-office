import type { IqrhDimension } from "./types";

export interface ProfileInput { globalScore:number; balanceIndex:number; icrScore:number; scores:Record<IqrhDimension,number>; situations:readonly string[] }
export interface ProfileCalculation { primaryName:string; secondaryName:string; primaryScore:number; secondaryScore:number; primaryConfidence:number; secondaryConfidence:number; signature:string; scores:Record<string,number>; irisContext:Record<string,string|number> }
const has=(situations:readonly string[], value:string)=>situations.some(item=>item.toLowerCase().includes(value.toLowerCase()));
export class ProfileCalculationService { static calculate(input:ProfileInput):ProfileCalculation { const s=input.scores; const scores:Record<string,number>={"Le Connecteur":(s.SOCIAL>=80?35:0)+(s.AFFECTIVE>=60?20:0)+(input.balanceIndex>=61?15:0),"L'Ancre":(s.AFFECTIVE>=70?35:0)+(s.SELF>=70?35:0),"Le Bâtisseur":(s.PROFESSIONAL>=80?45:0)+(s.SELF>=60?20:0),"Le Protecteur":(s.AFFECTIVE>=60?20:0)+(has(input.situations,"Parent")?30:0)+(has(input.situations,"Aidant")?30:0)+(has(input.situations,"Manager")?20:0),"Le Résilient":(input.icrScore>=41?35:0)+(input.globalScore>=50?25:0)+(input.balanceIndex>=61?20:0),"L'Explorateur":(s.SOCIAL>=70?30:0)+(s.SENTIMENTAL>=40?15:0)+(has(input.situations,"Célibataire")||has(input.situations,"Étudiant")?35:0),"Le Chercheur d'équilibre":input.balanceIndex>=81?70:0,"Le Soliste":(s.SELF>=70?30:0)+(s.SOCIAL<60?30:0)+(has(input.situations,"Personne vivant seule")?30:0),"Le Suradapté":(s.AFFECTIVE>=40&&s.AFFECTIVE<70?15:0)+(s.SELF<60?35:0)+(has(input.situations,"Parent")||has(input.situations,"Manager")||has(input.situations,"Aidant")?30:0),"Le Réorganisateur":(has(input.situations,"Divorce")||has(input.situations,"Deuil")||has(input.situations,"Demandeur")||has(input.situations,"Création d'entreprise")?70:0),"L'Inspirant":Object.values(s).filter(value=>value>80).length>=4?80:0,"L'Équilibriste":(input.globalScore>=70?35:0)+(input.icrScore>=41?35:0)}; const ranked=Object.entries(scores).sort((a,b)=>b[1]-a[1]); const [primaryName,primaryScore]=ranked[0]!;const[secondaryName,secondaryScore]=ranked[1]!; const total=Math.max(1,Object.values(scores).reduce((sum,value)=>sum+value,0));return{primaryName,secondaryName,primaryScore,secondaryScore,primaryConfidence:Math.round(primaryScore/total*100),secondaryConfidence:Math.round(secondaryScore/total*100),signature:`${primaryName.replace("Le ","").replace("L'","")}-${secondaryName.replace("Le ","").replace("L'","")}`,scores,irisContext:{primaryName,secondaryName,iqrhScore:input.globalScore,icrScore:input.icrScore,priorityDimension:Object.entries(s).sort((a,b)=>a[1]-b[1])[0]![0]}} } }
